# Environment variables for Chess Scan Backend

# Stockfish engine path (optional if in PATH)
STOCKFISH_PATH=stockfish

# Server configuration
HOST=0.0.0.0
PORT=3000

# CORS origins (comma-separated)
ALLOWED_ORIGINS=*
